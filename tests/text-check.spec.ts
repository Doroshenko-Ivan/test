// import { test, expect } from '../node_modules/@playwright/test';

// test('Перевірка тексту на сторінці', async ({ page }) => {
//      await page.goto('https://example.com');

//      // Перевірка, що на сторінці є текст "Example Domain"
//      await expect(page.locator('h1')).toHaveText('Example Domain');

//      // Перевірка, що будь-де є фраза (незалежно від тегу)
//      await expect(page.locator('body')).toContainText('This domain is for use in illustrative examples');
// });

